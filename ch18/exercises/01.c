/**
 * a. 存储类型：static，类型限定符：无，类型说明符：char，声明符：**lookup(int level)，初始化式：无；
 * b. 存储类型：无，类型限定符：volatile，类型说明符：unsigned long，声明符：io_flags，初始化式：无；
 * c.
 *  1. 存储类型：extern，类型限定符：无，类型说明符：char，声明符：*file_name[MAX_FILES]，初始化式：无；
 *  2. 存储类型：extern，类型限定符：无，类型说明符：char，声明符：path[]，初始化式：无；
 * d. 存储类型：static，类型限定符：const，类型说明符：char，声明符：token_buf[]，初始化式：""；
 */